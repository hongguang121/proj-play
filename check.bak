#lang racket

(define (check ls)
  (let loop ((ls ls)
             (res '()))             
    (if (null? ls)
        (reverse res)
        (loop (filter-not (λ (e) (eq? e (car ls))) (cdr ls))
              (cons (car ls) res)))))